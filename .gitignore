# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Local agents / AI tooling. GitHub Pages serves every tracked file, so a local
# launch config committed here is a public URL for no benefit.
.claude/
.Codex/
CLAUDE.md
AGENT.md
AGENTS.md

# Source photography and renders. 15.5MB of full-size PNGs that only exist to
# be optimized into assets/img/jewelry/*.webp, which IS tracked. GitHub Pages
# publishes every tracked file, so shipping the originals would put 15MB of
# unreferenced images on the CDN and in the repo history for no benefit.
assets/jewelry/

# Node / tooling
node_modules/
npm-debug.log*
yarn-error.log*
.cache/
dist/

# Local env
.env
.env.*
!.env.example

# Logs
*.log
